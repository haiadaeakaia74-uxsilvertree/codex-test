name: daily-x-post

on:
  schedule:
    - cron: "0 21 * * *"   # 日本 6:00（JST）= UTC 21:00
  workflow_dispatch:

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Show time
        run: date -u
      - name: Placeholder
        run: echo "ここでX投稿を実行する（まだ未設定）"
